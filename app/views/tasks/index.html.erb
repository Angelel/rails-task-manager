<h1>All tasks</h1>

<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task) %>
  <%= link_to 'delete a task', delete_path(task), method: :delete  %>
  <%= link_to 'edit a task', edit_path(task)  %>
  <p>--------</p>
<% end %>

<%= link_to "New task", new_task_path %>


